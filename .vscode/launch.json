{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "java",
        "name": "Launch UNO",
        "request": "launch",
        "mainClass": "unoproject2.Main",
        // If you want to run from bin, specify it (though the Java extension often detects it automatically).
        "cwd": "${workspaceFolder}/bin",
        // Pre-launch step to ensure we re-compile and copy resources
        "preLaunchTask": "Build"
      }
    ]
  }